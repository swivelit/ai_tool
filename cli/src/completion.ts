export function completion(shell: string): string { if (shell === 'bash') return '_swico_complete(){ COMPREPLY=( $(compgen -W "login logout whoami ask exec agent review resume doctor config mcp skills plugins completion" -- "${COMP_WORDS[COMP_CWORD]}") ); }; complete -F _swico_complete swico\n'; if (shell === 'zsh') return '#compdef swico\n_arguments "1:command:(login logout whoami ask exec agent review resume doctor config mcp skills plugins completion)"\n'; if (shell === 'fish') return 'complete -c swico -f -a "login logout whoami ask exec agent review resume doctor config mcp skills plugins completion"\n'; if (shell === 'powershell') return 'Register-ArgumentCompleter -CommandName swico -ScriptBlock { param($wordToComplete) "login","logout","whoami","ask","exec","agent","review","resume","doctor","config","mcp","skills","plugins","completion" | Where-Object { $_ -like "$wordToComplete*" } }\n'; throw new Error('Completion shell must be bash, zsh, fish, or powershell.') }
